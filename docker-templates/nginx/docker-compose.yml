#https://composerize.com/
version: '3.7'

services:
    nginx:
        container_name: nginx
        volumes:
            - "/docker-nas/docker-templates/nginx/nginx.conf:/etc/nginx/nginx.conf:ro"
            - "/docker-nas/docker-templates/nginx/sites:/docker-nas/docker-templates/nginx/sites:ro"
            - "/docker-nas/docker-data/nginx/.htpasswd:/etc/nginx/.htpasswd:ro"
            - "/docker-nas/docker-data/nginx/sites:/etc/nginx/templates:ro"
            - "/docker-nas/docker-data/nginx/logs:/etc/nginx/logs"
            - "/docker-nas/docker-data/letsencrypt:/etc/letsencrypt"
            - "/docker-nas/docker-data/certbot:/var/www/certbot"
        networks:
            - backend
        env_file:
            - /docker-nas/docker-templates/nginx/.env
        environment:
            - GLOBAL_DOMAIN
            - OCTOPRINT_PATH
            - HOMEASSISTANT_URI
        ports:
            - '443:443/tcp'
            - '80:80/tcp'

        restart: unless-stopped
        image: 'nginx:latest'